import React from 'react';
import { Title, Subheading } from 'react-native-paper';
import Base from '~/layout/Base';

export default () => (
  <Base>
    <Title>Bem vindo ao template</Title>
    <Subheading>Com typescript + paper</Subheading>
  </Base>
);
